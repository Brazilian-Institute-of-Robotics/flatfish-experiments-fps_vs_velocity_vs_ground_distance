Folder with data files extracted from flatfish logs created in 20151119-0935.

The data files are of type CSV, which the colums represents: % frames overlap, final velocity, X axis velocity, Y axis velocity, Z axis velocity, distance to ground.
Each file is named according the number of frame is skipped, like sk<# skipped frames>_values. 



